- $action = isset($_POST['action']) ? $_POST['action'] : '';
  это нужно будет убрать
- я могу собрать всю сортировку что-бы работало все одновременно
- залить на бесплатный хостинг, скопировать базу данных себе в папку
  в проект